<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Mappers.groupNoticeMapper">
	
	<!-- 공지 생성 -->
	<insert id="insertNotice" parameterType="com.coduck.pond.groupNotice.vo.GroupNoticeVo">
		
	</insert>
	
	<!-- 공지 리스트 가져오기 -->
	<select id="selectNoticeList" resultType="com.coduck.pond.group.vo.GroupVo" parameterType="string">
		
	
	</select>
	
	
	<!-- 공지 가져오기-->
	<select id="selectGroup" resultType="com.coduck.pond.group.vo.GroupVo" parameterType="int">
	</select>
	<!-- 그룹 삭제 -->
	<delete id="deleteGroup" parameterType="int">
	</delete>
</mapper> 